{
    "name": "Opencode 2",
    "id": "opencode2",
    "version": "1.0.0",
    "description": "A feature for installing opencode2, the preview/beta build of the next major version of the opencode CLI tool",
    "documentationURL": "https://opencode.ai/v2/docs",
    "options": {
        "version": {
            "type": "string",
            "default": "beta",
            "description": "npm dist-tag ('beta', 'next', 'dev', 'tui-v2') or an explicit @opencode-ai/cli-linux-<arch> package version to install."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
